# transcribe.py dependencies — install with: pip install -r requirements.txt
#
# yt-dlp        → downloads audio from Instagram / TikTok / YouTube / X / etc.
# faster-whisper → local Whisper transcription (CPU-friendly, no API key).
#
# System requirement (NOT a pip package): ffmpeg must be installed and on PATH.
#   macOS:   brew install ffmpeg
#   Ubuntu:  sudo apt install ffmpeg
#   Windows: winget install Gyan.FFmpeg

yt-dlp>=2024.1.1
faster-whisper>=1.0.0
